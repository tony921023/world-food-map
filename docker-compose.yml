services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: webfinal
      POSTGRES_PASSWORD: webfinal
      POSTGRES_DB: webfinal
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
